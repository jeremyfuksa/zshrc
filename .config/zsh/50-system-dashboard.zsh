# Execute system dashboard script
SYSTEM_DASHBOARD_SCRIPT=~/.config/zsh/scripts/system-dashboard
if [[ -f "" && -x "" ]]; then
    ""
else
    echo "System dashboard script not found or not executable."
fi# Execute system dashboard script
if [[ -f ~/.config/zsh/scripts/system-dashboard ]]; then
    ~/.config/zsh/scripts/system-dashboard
fi 
